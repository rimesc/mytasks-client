import { OpaqueToken } from '@angular/core';

export const CLIENT_ID = new OpaqueToken('auth.client');
export const DOMAIN = new OpaqueToken('auth.domain');
export const CALLBACK = new OpaqueToken('auth.callback');
export const LOGOUT = '/logout';
